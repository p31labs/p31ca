# P31 Backend — Termux-specific requirements
# Use instead of requirements.txt on Android/Termux
# Skips: neo4j (needs JVM), semantic-router (heavy ML), litellm
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
websockets>=13.0
httpx>=0.27.0
openai>=1.50.0
