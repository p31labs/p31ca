version: '3.8'

services:
  devcontainer:
    build:
      context: .devcontainer
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    networks:
      - p31-network
    depends_on:
      - neo4j
      - caddy

networks:
  p31-network:
    external: true
